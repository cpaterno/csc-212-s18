#include <iostream>

int main() {

    // read number of datasets
    int num_trials;
    std::cin >> num_trials;
    
    
    int k;
    int count;
    
    // loop over all datasets
    for (int i = 0; i < num_trials; i++) {

        // read k and count
        std::cin >> k;
        std::cin >> count;
        
        // TODO
        // your work goes here
        
        // TODO
        // print output for this case

    }

    return 0;
}
